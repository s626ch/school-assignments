// arrows to declare funcs instead of function, nice shorthand
const plantNeedsWater = (day) => {
    if (day === 'Wednesday') {
      return true;
    } else {
      return false;
    }
};
  